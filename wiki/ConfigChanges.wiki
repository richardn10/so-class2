#labels Phase-Deploy
#Make further changes necessary for Switched On system to function as intended

= Brief explanation =
This stage has two functions:
  # Copies config files into the system, overwriting where necessary
  # Copies other files needed by Switched On system

= Instructions =
  # Obtain the archive config file
  # Unzip the archive and the run config script:
{{{
To be provided
}}}